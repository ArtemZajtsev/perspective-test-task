// could have been generated with https://www.npmjs.com/package/joi-to-typescript to avoid errors, but in our case it is quite simple already
export type SortOrder = 'asc' | 'desc' | 'ascending' | 'descending';
